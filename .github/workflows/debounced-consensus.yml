name: Debounced Consensus

on:
  workflow_dispatch:

permissions:
  actions: write

concurrency:
  group: debounced-consensus
  cancel-in-progress: true

jobs:
  debounce:
    runs-on: ubuntu-latest
    steps:
      - name: Wait for quiet period
        run: sleep 600  # 10 minutes

      - name: Trigger consensus (backfill)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh workflow run consensus.yml -f mode=backfill -f batch_size=8 -f panel=all
